---
title: Next.js Plugin
description: "Configure the @taserjs/router-plugin/next bundler plugin for Next.js 15+ App Router. Options, disk artifact generation, and Turbopack/Webpack compilation."
icon: LayoutGrid
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";
import { TypeTable } from "@/components/type-table";

The `@taserjs/router-plugin/next` package integrates Taser into Next.js 15+ App Router compilation. It handles disk artifact generation, TypeScript ambient type generation, and Turbopack/Webpack extension aliases for subpath imports.

<Callout type="info" title="Looking for the Fullstack Next.js Guide?">
  For the complete architectural guide, React Server Components (RSC) patterns, and Server Actions
  data fetching recipes, see the [Next.js App Router Fullstack Guide](/docs/fullstack/nextjs).
</Callout>

---

## Installation

Install `@taserjs/router-plugin` as a dev dependency:

<Tabs items={["pnpm", "npm", "bun", "yarn"]} groupId="package-manager" persist>
  <Tab value="pnpm">
```bash 
pnpm add -D @taserjs/router-plugin

````
</Tab>
  <Tab value="npm">
```bash
npm install -D @taserjs/router-plugin

````

</Tab>
  <Tab value="bun">
```bash 
bun add -d @taserjs/router-plugin

````
  </Tab>
  <Tab value="yarn">
```bash
yarn add -D @taserjs/router-plugin

````

  </Tab>
</Tabs>

---

## Basic Configuration

Wrap your `next.config.ts` using the `createTaser` (or `withTaser`) wrapper:

```ts title="next.config.ts"
import createTaser from "@taserjs/router-plugin/next";
import type { NextConfig } from "next";

const withTaser = createTaser({
  basePath: "/api", // URL prefix where Taser routes dispatch
});

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default withTaser(nextConfig);
```

You can also pass a function-based Next.js configuration:

```ts title="next.config.ts"
import { withTaser } from "@taserjs/router-plugin/next";

export default withTaser({ basePath: "/api" })((phase, { defaultConfig }) => {
  return {
    ...defaultConfig,
    reactStrictMode: true,
  };
});
```

---

## Plugin Options

`createTaser` accepts the following configuration options:

```ts title="next.config.ts"
const withTaser = createTaser({
  basePath: "/api",
  routesDir: "src/routes",
  entryPath: "src/taser.ts",
  outDir: ".taser",
});
```

<TypeTable
  type={{
    basePath: {
      description: "URL prefix Taser dispatches under. Defaults to Next.js config basePath or '/'.",
      type: "string",
      default: "'/'",
    },
    routesDir: {
      description: "Path to your file-based routes directory.",
      type: "string",
      default: "'src/routes'",
    },
    entryPath: {
      description: "Path to the taser router instance definition file.",
      type: "string",
      default: "'src/taser.ts'",
    },
    outDir: {
      description: "Output directory where generated artifacts and types are written.",
      type: "string",
      default: "'.taser'",
    },
    watcher: {
      description: "File watcher configuration options for development mode.",
      type: "WatcherOptions",
      default: "undefined",
    },
  }}
/>

---

## How It Works Under the Hood

Next.js does not support serving virtual modules dynamically in runtime handlers. To provide zero-overhead integration, `@taserjs/router-plugin/next`:

1. **Monitors `src/routes/`**: In development (`next dev`), the plugin runs a lightweight background file watcher that synchronizes route changes instantly.
2. **Generates Disk Artifacts in `.taser/`**:
   - `.taser/manifest.js`: Static route radix manifest.
   - `.taser/entry.js`: Router entry initialization.
   - `.taser/app.js`: Compiled Taser app instance imported by your catch-all handler (`app/api/[[...slug]]/route.ts`).
   - `.taser/types/`: Ambient TypeScript definitions for end-to-end type safety.
3. **Configures Turbopack and Webpack**:
   - Configures extension aliases so Node.js ESM subpath imports (such as `#taserjs/router`) resolve across `.ts`, `.tsx`, and `.js` seamlessly.

---

## Next Steps

<Cards>
  <Card
    title="Next.js App Router Guide"
    href="/docs/fullstack/nextjs"
    description="Learn how to build fullstack applications with Next.js, RSC, and Taser."
  />
  <Card
    title="Vite Plugin"
    href="/docs/plugins/vite"
    description="Explore Taser's Vite plugin for standalone and fullstack applications."
  />
  <Card
    title="Nitro Module"
    href="/docs/plugins/nitro"
    description="Deploy Taser across edge, serverless, and multi-cloud runtimes."
  />
</Cards>

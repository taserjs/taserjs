---
title: Middleware Validation
description: "Validate incoming headers, auth tokens, and session context at the layout level before requests reach downstream REST API route handlers."
icon: ListFilter
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";

Validation in Taser is not limited to route handlers. Layout middleware can validate incoming request data and declare the exact shape of state it injects into child routes.

---

## State Inference & Injection

Layout middleware can validate incoming requests and inject typed state down the pipeline. Any object passed to `next({ ... })` is automatically merged into `ctx.state` and inferred across all child handlers:

```ts title="src/routes/orgs.ts"
import { reply } from "@taserjs/router";
import { t } from "#taserjs/router";

type Organization = {
  id: string;
  slug: string;
  plan: "free" | "pro" | "enterprise";
};

export const Middleware = t.middleware("orgs").use({
  handler: async (ctx, next) => {
    const orgSlug = ctx.headers.get("x-org-slug");
    if (!orgSlug) {
      return reply.badRequest({ message: "Missing x-org-slug header" });
    }

    const org: Organization | null = await ctx.db.findOrgBySlug(orgSlug);
    if (!org) {
      return reply.notFound({ message: "Organization not found" });
    }

    // TypeScript infers { organization: Organization } into ctx.state for all downstream routes:
    return next({ organization: org });
  },
});
```

---

## Validating Query, Params, and Body in Middleware

Middleware can declare `query`, `params`, or `body` schemas using Standard Schema to reject invalid requests early before hitting child routes:

```ts title="src/routes/api/v2.ts"
import { reply } from "@taserjs/router";
import { z } from "zod";
import { t } from "#taserjs/router";

export const Middleware = t.middleware("api_v2").use({
  query: z.object({
    apiKey: z.string().min(32),
  }),
  handler: async (ctx, next) => {
    // ctx.query.apiKey is validated
    const keyRecord = await ctx.db.findApiKey(ctx.query.apiKey);
    if (!keyRecord) {
      return reply.unauthorized({ message: "Invalid API key" });
    }

    return next({ tier: keyRecord.tier });
  },
});
```

---

## Multi-Step Middleware Pipelines

You can chain multiple `.use()` calls on a layout middleware. Each step enriches `ctx.state` and subsequent middleware steps immediately receive previously injected state:

```ts title="src/routes/billing.ts"
import { reply } from "@taserjs/router";
import { t } from "#taserjs/router";

export const Middleware = t
  .middleware("billing")
  // Step 1: Validate session & inject userId
  .use({
    handler: async (ctx, next) => {
      const token = ctx.headers.get("authorization")?.replace("Bearer ", "");
      if (!token) return reply.unauthorized();
      return next({ userId: "user_123" });
    },
  })
  // Step 2: Fetch subscription using userId from Step 1
  .use({
    handler: async (ctx, next) => {
      // ctx.state.userId is 100% typed from Step 1!
      const sub = await ctx.db.getSubscription(ctx.state.userId);
      return next({
        hasActiveSubscription: Boolean(sub?.active),
      });
    },
  });
```

---

## State Preconditions (`TRequires`)

When writing reusable standalone middlewares that assume certain state was already produced upstream (e.g. an authenticated user or tenant ID), declare a state requirement type via `defineMiddleware<TState, TRequires>`:

```ts title="src/middleware/require-subscription.ts"
import { defineMiddleware, reply } from "@taserjs/router";

type SubscriptionPrecondition = {
  hasActiveSubscription: boolean;
};

export const requireActivePlan = defineMiddleware<{}, SubscriptionPrecondition>({
  handler: (ctx, next) => {
    // TypeScript ensures ctx.state.hasActiveSubscription exists:
    if (!ctx.state.hasActiveSubscription) {
      return reply.forbidden({ message: "Active subscription required" });
    }

    return next();
  },
});
```

If you attach `.use(requireActivePlan)` to a route or layout that has not initialized `hasActiveSubscription` upstream, TypeScript flags a compile-time type mismatch.

---

## Next Steps

<Cards>
  <Card
    title="Handling Validation Errors"
    href="/docs/validation/handling-errors"
    description="Capture and format validation failure issues."
  />
  <Card
    title="Built-in Auth Middlewares"
    href="/docs/middleware/jwt-and-jwk"
    description="Use pre-built JWT and JWKS middlewares for token auth."
  />
</Cards>

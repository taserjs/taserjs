---
title: "@taserjs/router-plugin"
description: "Complete API reference and configuration options for @taserjs/router-plugin across Vite, Next.js App Router, and Nitro server environments."
icon: Layers
---

import { TypeTable } from "@/components/type-table";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Callout } from "fumadocs-ui/components/callout";

The `@taserjs/router-plugin` package provides framework adapters, virtual module compilation, and ambient TypeScript generation for Vite, Next.js, and Nitro.

---

## Exports Overview

| Subpath Export                    | Description                                       | Target Framework       |
| :-------------------------------- | :------------------------------------------------ | :--------------------- |
| `@taserjs/router-plugin/vite`     | Vite compiler plugin with virtual routing and HMR | Vite, TanStack Start   |
| `@taserjs/router-plugin/next`     | Next.js curried configuration wrapper             | Next.js App Router     |
| `@taserjs/router-plugin/nitro`    | Nitro server engine module                        | Standalone Nitro, Nuxt |
| `@taserjs/router-plugin/webpack`  | Webpack plugin                                    | Webpack                |
| `@taserjs/router-plugin/rspack`   | Rspack plugin                                     | Rspack                 |
| `@taserjs/router-plugin/rollup`   | Rollup plugin                                     | Rollup                 |
| `@taserjs/router-plugin/rolldown` | Rolldown plugin                                   | Rolldown               |
| `@taserjs/router-plugin/esbuild`  | Esbuild plugin                                    | Esbuild                |

<Callout type="info">
  The package root (`@taserjs/router-plugin`) is not part of the public API.
  `@taserjs/router-plugin/runtime` is reserved for generated app code.
</Callout>

---

## Vite Plugin (`@taserjs/router-plugin/vite`)

```ts
import { taser } from "@taserjs/router-plugin/vite";

export default defineConfig({
  plugins: [taser(options?)],
});
```

### `TaserPluginOptions`

<TypeTable
  type={{
    basePath: {
      description: "Root URL prefix Taser routes dispatch under.",
      type: "string",
      default: "'/'",
    },
    routesDir: {
      description: "Path to your file-based routes directory.",
      type: "string",
      default: "'src/routes'",
    },
    entryPath: {
      description: "Path to the taser router instance definition file. Alias for `entry`.",
      type: "string",
      default: "'src/taser.ts'",
    },
    entry: {
      description: "Alias for `entryPath`.",
      type: "string",
      default: "'src/taser.ts'",
    },
    serverEntryPath: {
      description:
        "Path to optional host server entry (e.g. 'src/server.ts' or 'src/server.node.ts'). Alias for `serverEntry`.",
      type: "string",
      default: "undefined",
    },
    serverEntry: {
      description: "Alias for `serverEntryPath`.",
      type: "string",
      default: "undefined",
    },
    server: {
      description:
        "Whether to enable the dev server middleware and generate production serve shim.",
      type: "boolean",
      default: "true",
    },
    standalone: {
      description:
        "When using Nitro, whether Taser replaces Nitro's app pipeline (zero h3/rou3) or registers as a standard Nitro handler.",
      type: "boolean",
      default: "true",
    },
    ignore: {
      description: "Glob patterns of files inside routesDir to exclude from routing.",
      type: "string[]",
      default: "['**/*.test.ts', '**/*.spec.ts', '**/*.d.ts']",
    },
    rootDir: {
      description: "Root directory of the project.",
      type: "string",
      default: "process.cwd()",
    },
  }}
/>

---

## Next.js Plugin (`@taserjs/router-plugin/next`)

```ts
import createTaser from "@taserjs/router-plugin/next";

const withTaser = createTaser(options?);
export default withTaser(nextConfig);
```

### `TaserNextOptions`

<TypeTable
  type={{
    serverDir: {
      description: "Root server directory containing your taser instance and routes folder.",
      type: "string",
      default: "'src/server'",
    },
    entry: {
      description: "Path or path alias to the taser router instance definition file.",
      type: "string",
      default: "'src/taser.ts'",
    },
    basePath: {
      description: "URL prefix Taser dispatches under.",
      type: "string",
      default: "'/'",
    },
    routesDir: {
      description: "Path to your file-based routes directory (defaults to `${serverDir}/routes`).",
      type: "string",
      default: "'src/server/routes'",
    },
    outDir: {
      description: "Output directory where generated disk artifacts and types are written.",
      type: "string",
      default: "'.taser'",
    },
    watcher: {
      description: "File watcher configuration options for development mode.",
      type: "WatcherOptions",
      default: "undefined",
    },
  }}
/>

---

## Nitro Module (`@taserjs/router-plugin/nitro`)

```ts
import { taser } from "@taserjs/router-plugin/nitro";

export default defineConfig({
  modules: [taser(options?)],
});
```

### `TaserNitroOptions`

<TypeTable
  type={{
    standalone: {
      description:
        "When true, Taser acts as the standalone app runner with zero overhead. When false, mounts as a sub-handler in Nitro.",
      type: "boolean",
      default: "true",
    },
    basePath: {
      description: "Root URL prefix Taser routes dispatch under.",
      type: "string",
      default: "'/'",
    },
    routesDir: {
      description: "Path to your file-based routes directory.",
      type: "string",
      default: "'src/routes'",
    },
    entryPath: {
      description: "Path to the taser router instance definition file. Alias for `entry`.",
      type: "string",
      default: "'src/taser.ts'",
    },
    entry: {
      description: "Alias for `entryPath`.",
      type: "string",
      default: "'src/taser.ts'",
    },
    serverEntryPath: {
      description:
        "Path to optional host server entry (e.g. 'src/server.ts'). Alias for `serverEntry`.",
      type: "string",
      default: "undefined",
    },
    serverEntry: {
      description: "Alias for `serverEntryPath`.",
      type: "string",
      default: "undefined",
    },
  }}
/>
